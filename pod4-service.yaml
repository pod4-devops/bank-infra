apiVersion: v1
kind: Service
metadata:
  name: pod4-service
  namespace: default
  labels:
    app: pod4
spec:
  selector:
    app: pod4
  ports:
    - name: http
      port: 8080
      targetPort: 80
    - name: metrics
      port: 9113
      targetPort: 9113

